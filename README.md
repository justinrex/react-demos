# react-demos

Small React demo playground with each demo exposed as its own page inside one app.

## Scripts

- `pnpm install`
- `pnpm dev`
- `pnpm check`

## Structure

- `src/pages`: individual demo pages
- `src/routes.tsx`: route registry
- `src/components`: shared UI for the demo shell
